---
import { Image } from 'astro:assets';

interface Props {
  image?: ImageMetadata;
  title?: string;
  body?: string;
  url?: string;
  class?: string;
}

const { image, title, body, url, class: className } = Astro.props;
---
<div class:list={[className]}>
  <div class="bg-white h-full">
    <slot name="icon">
      {image && (
        <a href={url} class="bg-black block hover:text-primary overflow-hidden group p-3">
          <Image
            class="object-cover aspect-square rounded-lg"
            src={image}
            alt={title || ''}
          />
        </a>
      )}
    </slot>

    <slot name="meta" />

    <slot name="title">
      <a href={url} class="block hover:text-primary transition duration-300 text-lg m-2">
        {title}
      </a>
    </slot>

    <slot name="body">
      <p class="m-2">
        {body}
      </p>
    </slot>
  </div>
</div>
